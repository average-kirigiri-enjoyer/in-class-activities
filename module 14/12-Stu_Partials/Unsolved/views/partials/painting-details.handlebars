<section class="card">

  <div class="card-body">

    <a href="/painting/{{id}}">

      <header>{{title}}</header>
      <p>{{artist}}</p>
      <img src="/images/{{filename}}" alt="{{description}}">

    </a>
  </div>
</section>

